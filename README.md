# WIP
## A Proper Read Me will be created SoonTM.

### You will need Postgres version 17.5 or higher.
Create a `.env` file in the project root for your PostgreSQL connection details.
Example `.env` format:

```
DB_USER=postgres 
DB_PASS=password 
DB_URL=jdbc:postgresql://localhost:5432/exampleDB
```